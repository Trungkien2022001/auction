/* eslint-disable react-hooks/exhaustive-deps */

import './UserTransaction.scss'
import * as React from 'react';

export const UserTransaction = ({ currentUser, socket }) => {
 
  return (
    <div className='dashboard-container'>
      Giao dịch
    </div>
  );
}
